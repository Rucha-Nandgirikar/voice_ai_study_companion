# Extension UI (React Side Panel)

This is a Vite + React side panel for the Chrome extension.

## Build

```bash
cd extension/ui
npm install
npm run build
```

This outputs files to `extension/dist/`, which Chrome loads via `extension/manifest.json`.


